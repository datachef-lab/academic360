<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Exam Form Confirmation</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,wght@0,400;0,600;1,400&family=DM+Sans:wght@300;400;500;600&display=swap');

    *{margin:0;padding:0;box-sizing:border-box;}

    body { margin: 0; padding: 0; background: #f6f8fb; color: #0f172a; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Helvetica Neue", Arial, sans-serif; }

    .shell{
      max-width:560px;
      margin:44px auto;
      border-radius:12px;
      overflow:hidden;
      box-shadow:0 4px 32px rgba(0,0,0,0.09);
    }

    /* ── LOGO BAR ── */
    .logo-bar{
      background:#ffffff;
      padding:20px 40px;
      display:flex;
      align-items:center;
      gap:16px;
      border-bottom:3px solid #0f2d5e;
    }
    .logo-bar img{height:50px;width:auto;}
    .lb-name{
      font-family:'Source Serif 4',Georgia,serif;
      font-size:14px;
      font-weight:600;
      color:#0f1f3d;
      line-height:1.35;
    }
    .lb-dept{
      font-size:10px;
      font-weight:600;
      letter-spacing:0.16em;
      text-transform:uppercase;
      color:#2d5fa6;
      margin-top:4px;
    }

    /* ── HERO ── */
    .hero{
      background: #0f2d5e;
      background-image:
        radial-gradient(ellipse at 20% 50%, rgba(45,95,166,0.55) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 20%, rgba(15,60,110,0.6) 0%, transparent 55%);
      padding: 52px 40px 48px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    /* decorative arcs */
    .hero::before {
      content: '';
      position: absolute;
      width: 420px; height: 420px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.06);
      top: -160px; left: 50%;
      transform: translateX(-50%);
      pointer-events: none;
    }
    .hero::after {
      content: '';
      position: absolute;
      width: 260px; height: 260px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.05);
      bottom: -110px; right: -60px;
      pointer-events: none;
    }

    .check-wrap{
      width: 72px; height: 72px;
      border-radius: 50%;
      background: #22c55e;
      border: 2px solid rgba(255,255,255,0.25);
      display: flex; align-items: center; justify-content: center;
      margin: 0 auto 10px;
      position: relative; z-index: 1;
      box-shadow: 0 0 0 10px rgba(34,197,94,0.15), 0 4px 20px rgba(34,197,94,0.35);
    }

    /* animated pulse ring */
    .check-wrap::after {
      content: '';
      position: absolute;
      inset: -8px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.08);
    }

    .hero-tag{
      display: inline-block;
      background: rgba(255,255,255,0.1);
      border: 1px solid rgba(255,255,255,0.18);
      border-radius: 100px;
      padding: 5px 16px;
      font-size: 10.5px;
      font-weight: 600;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.75);
      margin: 20px 0 18px;
      position: relative; z-index: 1;
    }

    /* green dot before tag */
    .hero-tag::before {
      content: '';
      display: inline-block;
      width: 6px; height: 6px;
      border-radius: 50%;
      background: #6ee7a0;
      margin-right: 8px;
      vertical-align: middle;
      margin-top: -2px;
      box-shadow: 0 0 6px rgba(110,231,160,0.7);
    }

    .hero-title{
      font-family: 'Source Serif 4', Georgia, serif;
      font-size: 28px;
      font-weight: 600;
      color: #ffffff;
      line-height: 1.45;
      position: relative; z-index: 1;
    }
    .hero-title em{
      font-style: italic;
      color: #a8cafe;
    }

    .hero-sub {
      font-size: 13px;
      font-weight: 300;
      color: rgba(255,255,255,0.45);
      margin-top: 12px;
      letter-spacing: 0.03em;
      position: relative; z-index: 1;
    }

    /* ── BODY ── */
    .body{
      background:#ffffff;
      padding:40px 40px 36px;
    }

    .dear{
      font-family:'Source Serif 4',Georgia,serif;
      font-size:18px;
      color:#0f1f3d;
      margin-bottom:14px;
    }

    .para{
      font-size:14px;
      font-weight:300;
      line-height:1.9;
      color:#4a5568;
      margin-bottom:32px;
    }
    .para strong{
      font-weight:600;
      color:#0f1f3d;
    }

    /* ── CTA ── */
    .cta-wrap{
      text-align:center;
      margin-bottom:36px;
    }
    .cta{
      display:inline-block;
      background:#0f2d5e;
      color:#ffffff !important;
      text-decoration:none;
      font-size:12.5px;
      font-weight:600;
      letter-spacing:0.1em;
      text-transform:uppercase;
      padding:14px 40px;
      border-radius:6px;
      box-shadow:0 4px 18px rgba(15,45,94,0.25);
    }

    /* ── DIVIDER ── */
    .divider{
      border:none;
      border-top:1px solid #edf0f5;
      margin-bottom:24px;
    }

    /* ── SIGNOFF ── */
    .sf-warm{
      font-size:13.5px;
      font-weight:300;
      color:#7a8899;
      margin-bottom:6px;
    }
    .sf-name{
      font-family:'Source Serif 4',Georgia,serif;
      font-size:16px;
      font-weight:600;
      color:#0f1f3d;
    }
    .sf-sub{
      font-size:12px;
      color:#a0aec0;
      margin-top:3px;
    }

    /* ── FOOTER ── */
    .footer{
      background:#0a1f45;
      padding:18px 40px;
      text-align:center;
      border-top:1px solid rgba(255,255,255,0.08);
    }
    .footer p{
      font-size:12px;
      color:rgba(255,255,255,0.65);
      font-style:italic;
      letter-spacing:0.04em;
    }

    @media(max-width:560px){

  body {
    padding: 0 !important;
  }

  .shell{
    margin:0 !important;
    border-radius:0 !important;
  }

  .logo-bar,
  .hero,
  .body,
  .footer{
    padding-left:18px !important;
    padding-right:18px !important;
  }

  .logo-bar{
    padding-top:16px !important;
    padding-bottom:16px !important;
  }

  .logo-bar img{
    height:36px !important;
  }

  .lb-name{
    font-size:12px !important;
  }

  .lb-dept{
    font-size:9px !important;
  }

  /* HERO FIX */
  .hero{
    padding:36px 14px 32px !important;
  }

  .hero-title{
    font-size:22px !important;
    line-height:1.35 !important;
  }

  .hero-sub{
    font-size:12px !important;
  }

  .hero-tag{
    font-size:9px !important;
    padding:4px 12px !important;
  }

  .check-wrap{
    width:60px !important;
    height:60px !important;
  }

  /* BODY */
  .body{
    padding:30px 18px !important;
  }

  .para{
    font-size:13px !important;
    line-height:1.75 !important;
  }

  /* CTA */
  .cta{
    padding:12px 28px !important;
    font-size:11.5px !important;
  }
}

  </style>
</head>
<body style="background: #f6f8fb; color: #0f172a; padding: 12px 0;">
<div class="shell" style="border: 1px solid bisque;">

  <!-- LOGO BAR -->
  <div class="logo-bar" style="display: flex; align-items: center; gap: 24px;">
    <img src="https://besc.academic360.app/api/api/v1/settings/file/4" alt="BESC Logo"/>
    <div style="margin-left: 24px;">
      <div class="lb-name">The Bhawanipur Education Society College</div>
      <div class="lb-dept">Examination Department</div>
    </div>
  </div>

  <!-- HERO -->
  <div class="hero">
    <div style="
  width:72px;
  height:72px;
  border-radius:50%;
  background:#22c55e;
  margin:0 auto 10px;
  text-align:center;
  line-height:72px;
  font-size:34px;
  color:#ffffff;
  font-weight:bold;
">
  ✓
</div>

    <div class="hero-tag">Submission Confirmed</div>
    <div class="hero-title">Your Examination Form<br/>has been <em>successfully submitted.</em></div>
    <p class="hero-sub">Calcutta University &nbsp;·&nbsp; Academic Year <%= content.dtoTemplateData.academicYearName %></p>
  </div>

  <!-- BODY -->
  <div class="body">

    <p class="dear">Dear <%= content.dtoTemplateData.name %>,</p>
    <p class="para">
      You have successfully submitted your <strong>Calcutta University Examination <%= content.dtoTemplateData.academicYear %> Form</strong>
      generated from the Calcutta University portal for <strong><%= content.dtoTemplateData.semester %></strong>
      through your Online profile.
    </p>

    <div class="cta-wrap">
      <a href="<%= content.dtoTemplateData.studentConsoleUrl %>" target="_blank" class="cta">Access Student Console →</a>
    </div>

    <hr class="divider"/>

    <p class="sf-warm">Best regards,</p>
    <p class="sf-name">Examination Department</p>
    <p class="sf-sub">The Bhawanipur Education Society College</p>

  </div>

  <!-- FOOTER -->
  <div class="footer">
    <p>This is a system-generated email. Please do not reply.</p>
  </div>

</div>
</body>
</html>