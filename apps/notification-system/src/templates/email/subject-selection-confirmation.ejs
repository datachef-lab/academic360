<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= content && content.subject ? content.subject : "Confirmation of Semester-wise Subject Selection" %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
    <style>
      body { background: #f6f8fb; }
      .email-card { border: 1px solid #e6e8ee; }
      .muted { color: #64748b; }
      .header-bar { background:#0f172a;color:#fff }
      .table-sm th, .table-sm td { padding: .5rem .6rem; font-size: 13px; }
    </style>
  </head>
  <body class="py-4">
    <div class="container" style="max-width: 760px;">
      <div class="card email-card shadow-sm">
        <div class="card-header header-bar fw-semibold">Academic360 Notifications</div>
        <div class="card-body">
          <p class="mb-2">Dear <%= (user && user.name) ? user.name : "Student" %>,</p>
          <p class="mb-3">
            Congratulations! You have successfully submitted your <strong>Semester-wise Subject Selection</strong>
            under the Curriculum &amp; Credit Framework (CCF) for the academic year
            <strong><%= content && content.academicYear ? content.academicYear : "" %></strong>.
          </p>
          <p class="mb-3">For your ready reference, a summary of subjects to be studied by you from <strong>Semesters I to IV</strong> (including the subjects selected by you) is provided below:</p>

          <div class="table-responsive">
            <table class="table table-bordered table-striped table-sm align-middle mb-3">
              <thead class="table-light">
                <tr>
                  <th scope="col">Category</th>
                  <th scope="col">Semester I</th>
                  <th scope="col">Semester II</th>
                  <th scope="col">Semester III</th>
                  <th scope="col">Semester IV</th>
                </tr>
              </thead>
              <tbody>
                <% const rows = content && content.subjectsByCategory ? content.subjectsByCategory : {}; %>
                <% const categories = ["Minor", "IDC", "AEC", "CVAC"]; %>
                <% categories.forEach(function(cat) { %>
                  <tr>
                    <td class="fw-semibold"><%= cat %></td>
                    <td><%= rows[cat] && rows[cat]["I"] ? rows[cat]["I"] : "" %></td>
                    <td><%= rows[cat] && rows[cat]["II"] ? rows[cat]["II"] : "" %></td>
                    <td><%= rows[cat] && rows[cat]["III"] ? rows[cat]["III"] : "" %></td>
                    <td><%= rows[cat] && rows[cat]["IV"] ? rows[cat]["IV"] : "" %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>

          <p class="muted mb-4">Kindly retain this email for your reference during the course of your study.</p>
          <p class="mb-0">Best regards,<br />Admission Department<br />The Bhawanipur Education Society College</p>
          <p class="muted mt-3 mb-0">This is a system-generated email. Please do not reply to this message.</p>
        </div>
        <div class="card-footer text-muted small">&copy; <%= new Date().getFullYear() %> Academic360</div>
      </div>
    </div>
  </body>
  </html>

