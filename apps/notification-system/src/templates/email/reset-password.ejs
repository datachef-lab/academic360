<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= content && content.subject ? content.subject : "Reset your password" %></title>
    <style>
      body { margin:0; padding:0; background:#f6f8fb; color:#0f172a; font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Oxygen,Ubuntu,Cantarell,"Helvetica Neue",Arial,sans-serif; }
      .wrapper { width:100%; background:#f6f8fb; padding:24px 12px; }
      .container { max-width:640px; margin:0 auto; background:#ffffff; border:1px solid #e6e8ee; border-radius:12px; overflow:hidden; box-shadow:0 2px 6px rgba(2,6,23,0.06); }
      .header-bar { padding:16px 20px; background:#1e40af; color:#ffffff; font-weight:600; font-size:16px; text-align:center; }
      .content { padding:24px 20px 8px 20px; }
      .greeting { margin:0 0 8px 0; font-size:14px; }
      .lead { margin:0 0 16px 0; font-size:14px; line-height:1.6; color:#334155; }
      .note { padding:12px 14px; border-radius:10px; margin:12px 0; text-align:left; }
      .info { background:#eff6ff; border:1px solid #bfdbfe; color:#1e40af; }
      .footer { padding:16px 20px 20px 20px; color:#64748b; font-size:12px; text-align:center; border-top:1px solid #e6e8ee; }
      .btn { display:inline-block; background:#1e40af; color:#ffffff !important; text-decoration:none; padding:12px 18px; border-radius:10px; font-weight:700; }
      .token { font-family:"Courier New", monospace; font-weight:700; background:#f8fafc; border:1px solid #e2e8f0; border-radius:8px; padding:8px 12px; display:inline-block; }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="container">
        <div class="header-bar">Reset your password</div>
        <div class="content">
          <% if (user && user.name) { %>
            <p class="greeting">Hello <strong><%= user.name %></strong>,</p>
          <% } else if (content && (content.greetingName || (content.templateData && (content.templateData.greetingName || content.templateData.Name)))) { %>
            <p class="greeting">Hello <strong><%= content.greetingName || (content.templateData && (content.templateData.greetingName || content.templateData.Name)) %></strong>,</p>
          <% } else { %>
            <p class="greeting">Hello,</p>
          <% } %>

          <p class="lead">We received a request to reset the password for your account. You can reset it using the button below. This link will expire in 15 minutes.</p>

          <% if (content && (content.resetLink || (content.templateData && content.templateData.resetLink))) { %>
            <p style="text-align:center; margin:20px 0;">
              <a href="<%= content.resetLink || (content.templateData && content.templateData.resetLink) %>" class="btn">Reset Password</a>
            </p>
          <% } %>

          <% if (content && (content.token || (content.templateData && (content.templateData.token || content.templateData.Code)))) { %>
            <div class="note info">
              If the button doesn't work, use this token on the reset page:
              <div class="token"><%= content.token || (content.templateData && (content.templateData.token || content.templateData.Code)) %></div>
            </div>
          <% } %>

          <div class="note">
            If you did not request a password reset, you can safely ignore this email.
          </div>
        </div>
        <div class="footer">
          <div class="brand">The Bhawanipur Education Society College</div>
          <div>This is an automated message. Please do not reply to this email.</div>
          <div style="margin-top:6px;">&copy; <%= new Date().getFullYear() %> The Bhawanipur Education Society College. All rights reserved.</div>
        </div>
      </div>
    </div>
  </body>
  </html>

