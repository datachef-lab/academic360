# VCS
.git
.gitignore

# Dependencies (reinstalled in image)
node_modules
.pnpm-store
.pnp*

# Turbo / build artifacts (rebuilt in image)
**/.turbo
.turbo
apps/**/dist
apps/**/.next
packages/**/dist

# Logs & local data
*.log
apps/**/logs
**/uploads
coverage

# Env (inject at runtime / build-args)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode
.idea

# Mobile / non-docker apps (optional slim context)
apps/student-console-mobile
